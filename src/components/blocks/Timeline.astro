---
import { tinaFieldFrom } from '../../lib/tina-fields';

const { data } = Astro.props;
const bgClass = data.bg && data.bg !== 'none' ? ` bg-${data.bg}` : '';
const items = (data.items ?? []).filter((i: unknown) => i !== null);
const titleLines: string[] = (data.title ?? '').split('\n');
---
<section class={`section${bgClass}`}>
	<div class="wrap">
		{data.eyebrow && <p class="eyebrow reveal" data-tina-field={tinaFieldFrom(data, 'eyebrow')}>{data.eyebrow}</p>}
		{data.title && <h2 class="display-m reveal" style="margin-top:14px;max-width:20ch;" data-tina-field={tinaFieldFrom(data, 'title')}>
			{titleLines.map((line, i) => (<Fragment>{i > 0 && <br />}{line}</Fragment>))}
		</h2>}
		{data.lead && <p class="lead reveal d1" style="margin-top:14px;" data-tina-field={tinaFieldFrom(data, 'lead')}>{data.lead}</p>}
		<ol class="timeline">
			{items.map((item: any) => (
				<li class="reveal" data-tina-field={tinaFieldFrom(item)}>
					{item.kicker && <span class="tl-k" data-tina-field={tinaFieldFrom(item, 'kicker')}>{item.kicker}</span>}
					<h3 data-tina-field={tinaFieldFrom(item, 'title')}>{item.title}</h3>
					<p data-tina-field={tinaFieldFrom(item, 'text')}>{item.text}</p>
					{item.piece && <span class="tl-piece" data-tina-field={tinaFieldFrom(item, 'piece')}>{item.piece}</span>}
				</li>
			))}
		</ol>
	</div>
</section>
